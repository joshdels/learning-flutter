import 'package:flutter/material.dart';

class TodoText extends StatelessWidget {
  const TodoText({super.key});

  @override
  Widget build(BuildContext context) {
    return Text('Todos remaining mate');
  }
}
